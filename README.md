# PokéControl Center

Panel estático con estado de agentes, canales y misiones. Se genera corriendo desde la raíz del workspace:

```bash
node scripts/build-control-center.mjs
```

Ese script actualiza `index.html` con los datos más recientes. Luego solo queda publicar el contenido de esta carpeta.
